/**
 * Starts an allowlisted Next-owned Workflow through the target World selected
 * by `withWorkflow()`.
 */
export {
  startNextWorkflow,
  type NextWorkflowRunHandle,
} from "#internal/workflow/next-workflow-target.js";
