---
"eve": minor
---

Replace numeric extension capability contracts with consumer-owned eve semver ranges checked against each distribution's build version. Existing extensions must upgrade their build-time eve and rebuild to emit the new compatibility manifest format.
