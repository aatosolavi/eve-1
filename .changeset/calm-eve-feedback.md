---
"eve": patch
---

Improve the local authoring loop with automatic per-session diagnostic logs, direct post-init startup, inherited shell credentials, and newest-first model results. Use `eve logs ls` to list local logs and `eve logs [session-id]` to follow the most recently used or exact session.
