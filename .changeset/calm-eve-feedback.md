---
"eve": patch
---

Improve the local authoring loop with one automatic diagnostic log per `eve dev` invocation, direct post-init startup, inherited shell credentials, and newest-first model results. Use `eve logs ls` to list invocation logs and `eve logs [log-id]` to follow the most recent or an exact log.
