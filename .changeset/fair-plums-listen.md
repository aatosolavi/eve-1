---
"eve": patch
---

Record one chronological diagnostic log for each owned `eve dev` invocation, including process and sandbox output plus Workflow-reconciled lifecycle and eve events. Use `eve logs ls` to list invocation logs and `eve logs [log-id]` to follow the newest or an exact log.
