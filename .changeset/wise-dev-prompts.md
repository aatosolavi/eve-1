---
"eve": patch
---

Invoke an agent without a TUI using `eve invoke`, with pretty JSON results and an optional `--wait` for terminal or blocking events. Durable session coordinates support follow-up turns, human input, authorization, interrupted waits, and machine discovery through `--json-schema`.
