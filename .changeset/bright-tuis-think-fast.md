---
"eve": patch
---

Improve the `eve dev` TUI with release-sorted model selection, reasoning and Gateway Fast mode controls, semantic and coalesced tool activity, parser-backed Markdown, and concise errors backed by local diagnostic logs.
